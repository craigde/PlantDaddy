# Database (Required)
# Railway provides this automatically if you add a PostgreSQL plugin
DATABASE_URL=postgresql://user:password@host:5432/database

# Session Secret (Required)
# Generate a random string: openssl rand -base64 32
SESSION_SECRET=your-secret-key-here

# Optional: Pushover Notifications
# Get these from https://pushover.net/
PUSHOVER_APP_TOKEN=
PUSHOVER_USER_KEY=

# Optional: Object Storage paths (for file uploads)
# These default to ./uploads if not set
PRIVATE_OBJECT_DIR=./uploads
PUBLIC_OBJECT_SEARCH_PATHS=./uploads

# Auto-set by Railway
# PORT=
# NODE_ENV=
