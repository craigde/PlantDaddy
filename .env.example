# Database (Required)
# Railway provides this automatically if you add a PostgreSQL plugin
DATABASE_URL=postgresql://user:password@host:5432/database

# Session Secret (Required)
# Generate a random string: openssl rand -base64 32
SESSION_SECRET=your-secret-key-here

# Optional: Pushover Notifications
# Get these from https://pushover.net/
PUSHOVER_APP_TOKEN=
PUSHOVER_USER_KEY=

# Cloudflare R2 Storage (Recommended for persistent image storage)
# Get these from Cloudflare Dashboard > R2 > Manage R2 API Tokens
# Without these, images are stored locally and will be lost on redeploy
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=plantdaddy

# Auto-set by Railway
# PORT=
# NODE_ENV=
