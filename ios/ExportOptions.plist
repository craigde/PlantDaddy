<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!--
        ExportOptions.plist for TestFlight / App Store Distribution

        Use this when exporting an archive for TestFlight upload.
        This tells Xcode to use App Store Distribution signing,
        which does NOT require registered device UDIDs.

        Usage (command line):
            xcodebuild -exportArchive \
                -archivePath build/PlantDaddy.xcarchive \
                -exportPath build/export \
                -exportOptionsPlist ExportOptions.plist

        Or in Xcode:
            Product > Archive > Distribute App > App Store Connect
    -->

    <!-- Distribution method: app-store for TestFlight and App Store -->
    <key>method</key>
    <string>app-store</string>

    <!-- Let Xcode manage signing automatically -->
    <key>signingStyle</key>
    <string>automatic</string>

    <!-- Upload to App Store Connect (TestFlight) -->
    <key>destination</key>
    <string>upload</string>

    <!-- Strip Swift symbols to reduce binary size -->
    <key>stripSwiftSymbols</key>
    <true/>

    <!-- Upload symbols for crash reporting -->
    <key>uploadSymbols</key>
    <true/>

    <!--
        If automatic signing fails, switch to manual signing:

        1. Uncomment the block below
        2. Replace YOUR_TEAM_ID with your Apple Developer Team ID
           (found at https://developer.apple.com/account -> Membership Details)
        3. Set signingStyle above to "manual"
    -->
    <!--
    <key>teamID</key>
    <string>YOUR_TEAM_ID</string>

    <key>signingCertificate</key>
    <string>Apple Distribution</string>

    <key>provisioningProfiles</key>
    <dict>
        <key>com.craigde.PlantDaddy</key>
        <string>PlantDaddy App Store Profile</string>
    </dict>
    -->
</dict>
</plist>
